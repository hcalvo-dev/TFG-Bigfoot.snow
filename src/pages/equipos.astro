---
export const prerender = false;
import Layout from '../layouts/Layout.astro';
import HeroWaveDynamic from '../components/HeroWaveDynamic';
import TiendaComponent from '../components/Tienda/ReservarProductos';
import { checkSession } from '../../utils/checkSession';

const session = checkSession(Astro.request);
---

<Layout title="BIGFOOT : Productos">
  <main>
    <HeroWaveDynamic client:only="react" />

    <!-- Sección de ventajas -->
    <section id="contenido" class="relative w-full min-h-[81vh]">
      <TiendaComponent client:only="react" session={JSON.stringify(session)} />
    </section>
  </main>
</Layout>
